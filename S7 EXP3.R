customer <- c(1,2,3)
age <- c(28,35,42)
gender <- c("Female","Male","Female")
income <- c(50000,60000,75000)

demo <- data.frame(Customer_ID=customer,
                   Age=age,
                   Gender=gender,
                   Income=income)

demo